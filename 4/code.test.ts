import { describe, expect, it } from "vitest";
import { day4part1, day4part2 } from "./code";

const testInput = ``;

describe("part 1", () => {
  it("works on test input", () => {
    expect(day4part1(testInput)).toBe(NaN);
  });
});
describe("part 2", () => {
  it("works on test input", async () => {
    expect(day4part2(testInput)).toBe(NaN);
  });
});
